/** @type {import('tailwindcss').Config} */
module.exports = {
  // Scan the single page (includes class names used inside the inline <script>)
  content: ['./index.html'],
  theme: {
    extend: {},
  },
  plugins: [],
};
