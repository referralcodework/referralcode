{
  "name": "referralcode-site",
  "version": "1.0.0",
  "private": true,
  "description": "Official website for $REFERRAL — referralcode.work",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
